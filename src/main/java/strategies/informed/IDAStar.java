package strategies.informed;

import game.Board;
import strategies.SearchStrategy;
import strategies.utils.Path;

public class IDAStar extends SearchStrategy {
    public IDAStar(final Board board) {
        super(board);
    }

    @Override
    public Path findSolution() {
        return null;
    }
}
