package map.tiles;

/**
 * This object can make ONLY push movements
 *
 * When a box A has for example a box B in his left, whenever pusher tries to push box A to the left
 * then box B behaves as a rockTile
 */
public class PusherTile implements Tile {
}
